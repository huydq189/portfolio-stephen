---
title: 'work.ts'
summary: "I'm Stephen. I specialize in JavaScript and TypeScript"
---

```ts
// Work Information
const PROFESSION = 'Full-Stack Developer'

const employer = [
  'OCEAN NETWORK EXPRESS', // Middle -> Senior
  'CB/I Digital', // Junior -> Middle
  'VNPT-IT' // Intern -> Junior
]

// Skills
const languages = ['TypeScript', 'JavaScript', 'Go']
const frameworks = ['NestJS', 'ExpressJS', 'React', 'Next.Js', 'Echo']
const databases = ['Mongodb', 'Postgresql', 'Redis']
const deployments = ['Nginx', 'Docker', 'Pm2', 'Kubernetes', 'GitHub Actions']
const realtimeLibraries = ['socket.io','WebRTC']
const cloudProviders = ['AWS', 'GCP']
```
